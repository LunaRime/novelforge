/**
 * NovelForge 共享常量定义
 *
 * 集中管理跨模块使用的魔法数字、默认值和配置常量。
 * 所有模块应从此文件导入，而非直接硬编码。
 */

/** 每章计划字数的默认值 */
export const DEFAULT_WORDS_PER_CHAPTER = 3000

/** 计划总章数的默认值 */
export const DEFAULT_TOTAL_CHAPTERS = 100

/** Toast 通知自动消失时间（毫秒） */
export const TOAST_AUTO_DISMISS_MS = 3000
